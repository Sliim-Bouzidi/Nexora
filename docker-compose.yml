services:
  nexora:
    image: slimbouzidi/nexora:latest
    container_name: nexora
    expose:
      - "3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY:-sk_test_APnUT8y9QPz5keMq9zqjEeE8MP6oo7xti7VhHt0giU}
      DATABASE_URL: ${DATABASE_URL:-postgresql://nexora:nexorapassword@postgres:5432/nexora_cms}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  nginx:
    image: nginx:1.27-alpine
    container_name: nexora-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - nexora
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: nexora-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: nexora
      POSTGRES_PASSWORD: nexorapassword
      POSTGRES_DB: nexora_cms
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nexora -d nexora_cms"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
