services:
  nexora:
    build: .
    image: nexora:1.0
    container_name: nexora
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
