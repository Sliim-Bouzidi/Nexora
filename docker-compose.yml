services:
  nexora:
    image: slimbouzidi/nexora:latest
    container_name: nexora
    expose:
      - "3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY}
      DATABASE_URL: ${DATABASE_URL:-postgresql://nexora:nexorapassword@postgres:5432/nexora_cms}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  nexora-dashboard:
    image: slimbouzidi/nexora-dashboard:latest
    container_name: nexora-dashboard
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY}
    restart: unless-stopped

  nginx:
    image: nginx:1.27-alpine
    container_name: nexora-nginx
    ports:
      - "80:80"
      - "3001:3001"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - nexora
      - nexora-dashboard
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: nexora-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: nexora
      POSTGRES_PASSWORD: nexorapassword
      POSTGRES_DB: nexora_cms
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nexora -d nexora_cms"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
